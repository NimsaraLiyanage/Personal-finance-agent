/**
 * The app's content column.
 *
 * One constant, used by the nav bar and every page that isn't a reading
 * column — so the logo sits directly above the page heading instead of the two
 * drifting apart. The nav's *background* still spans the window; only what's
 * inside it lines up with the content.
 */
export const CONTAINER = 'mx-auto w-full max-w-[86rem] px-4 sm:px-6';
